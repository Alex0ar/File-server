# File Server

File Server is a Java client-server project that simulates a small remote file storage service. The client sends requests over a socket connection, and the server handles file upload, download, deletion, and lookup operations.

The project stores files on the server side and assigns identifiers so files can be accessed either by name or by ID. It also includes a client-side data folder for files that are uploaded to or downloaded from the server.
